# 변경 기록

이 문서는 포트폴리오의 주요 버전별 기능과 검증 방식 변경을 기록합니다.

## 0.3.0 - 2026-09-07

설계·설치·계약·검증 근거를 보강한 포트폴리오 버전입니다.

### 추가

- 모든 JSON 출력에 `schema_version`과 `command`를 포함하는 JSON 1.0 계약
- `tree`의 확장자별 파일 통계
- `compare --strict`의 누락·추가 파일 엄격 검증
- 반복 가능한 ignore 패턴과 실행 가능한 합성 업무 시나리오
- 설치된 패키지에서 사용할 수 있는 PEP 561 타입 정보

### 변경

- 기대 파일 목록을 POSIX 상대경로로 정규화
- 절대경로, 드라이브 경로와 `..` 상위 경로 참조를 입력 오류로 처리
- `tree --mode`의 가능한 값을 도움말과 인자 검사 단계에서 제한

### 검증

- Ubuntu의 Python 3.11, 3.12, 3.13과 Windows의 Python 3.13에서 CI 실행
- 문장·분기 커버리지 90% 이상 요구
- 빌드한 wheel을 격리 환경에 설치해 두 CLI 진입점, JSON 계약, 종료 코드를 검증
- sdist에서 템플릿 전용 라이선스를 제외하고 두 배포물의 권리 유보 안내를 검증

### 호환성

- Python 3.11 이상이 필요합니다.
- 런타임 외부 의존성은 없습니다.
- JSON 소비자는 `schema_version`의 주 버전을 확인해야 합니다.

### 이용 안내

이 저장소는 오픈소스 라이선스를 제공하지 않으며 모든 권리를 유보합니다. 버전 표기는 코드나 문서에 대한 별도의 이용 권한을 부여하지 않습니다.
